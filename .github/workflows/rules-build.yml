name: Fetch, Convert and Release SRS

on:
  push:
    branches:
      - main
    paths:
      - 'subscriptions.txt'
      - '.github/workflows/**'
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python Dependencies
        run: pip install pyyaml requests

      - name: Install Latest sing-box
        run: |
          LATEST_URL=$(curl -sL https://api.github.com/repos/SagerNet/sing-box/releases/latest | grep "browser_download_url" | grep "linux-amd64.tar.gz" | cut -d '"' -f 4)
          wget -qO sing-box.tar.gz $LATEST_URL
          tar -xzf sing-box.tar.gz
          sudo mv sing-box-*/sing-box /usr/local/bin/

      - name: Process URLs and Convert to JSON
        run: |
          mkdir -p srs_output
          
          cat << 'EOF' > convert.py
          import os, json, yaml, requests

          def process_url(name, url):
              print(f"Downloading {name}...")
              try:
                  resp = requests.get(url, timeout=15)
                  resp.raise_for_status()
                  content = resp.text
              except Exception as e:
                  print(f"Error downloading {name}: {e}")
                  return
                  
              out_json_path = f"srs_output/{name}.json"
              
              try:
                  data = json.loads(content)
                  if "rules" in data:
                      with open(out_json_path, "w", encoding="utf-8") as f:
                          json.dump(data, f, indent=2, ensure_ascii=False)
                      return
              except json.JSONDecodeError:
                  pass
                  
              try:
                  data = yaml.safe_load(content)
                  if isinstance(data, dict) and "payload" in data:
                      payload = data["payload"]
                  else:
                      payload = [line for line in content.splitlines() if line.strip() and not line.strip().startswith("#")]
              except yaml.YAMLError:
                  payload = [line for line in content.splitlines() if line.strip() and not line.strip().startswith("#")]

              sb_rules = {}
              mapping = {
                  "DOMAIN-SUFFIX": "domain_suffix",
                  "DOMAIN": "domain",
                  "DOMAIN-KEYWORD": "domain_keyword",
                  "DOMAIN-REGEX": "domain_regex",
                  "IP-CIDR": "ip_cidr",
                  "IP-CIDR6": "ip_cidr",
                  "SRC-IP-CIDR": "source_ip_cidr",
                  "PROCESS-NAME": "process_name",
                  "DST-PORT": "port",
                  "SRC-PORT": "source_port"
              }

              for line in payload:
                  line = str(line).strip()
                  if not line or line.startswith("#"): continue
                  parts = [p.strip() for p in line.split(',', 1)]
                  if len(parts) >= 2:
                      r_type, r_val = parts[0].upper(), parts[1]
                      r_val = r_val.split(',')[0].strip() 
                      
                      if r_type in mapping:
                          key = mapping[r_type]
                          sb_rules.setdefault(key, []).append(r_val)

              if not sb_rules:
                  print(f"-> [{name}] No valid rules extracted.")
                  return

              final_json = {
                  "version": 2,
                  "rules": [sb_rules]
              }
              
              with open(out_json_path, "w", encoding="utf-8") as f:
                  json.dump(final_json, f, indent=2, ensure_ascii=False)
              print(f"-> [{name}] Success.")

          if os.path.exists("subscriptions.txt"):
              with open("subscriptions.txt", "r", encoding="utf-8") as f:
                  for line in f:
                      line = line.strip()
                      if line and not line.startswith("#"):
                          if "," in line:
                              name, url = line.split(",", 1)
                              process_url(name.strip(), url.strip())
          EOF
          
          python convert.py

      - name: Compile JSON to SRS
        run: |
          for file in srs_output/*.json; do
            if [ -f "$file" ]; then
              name=$(basename "$file" .json)
              sing-box rule-set compile "$file" -o "srs_output/${name}.srs"
            fi
          done

      # === 新增：清理旧的 latest 标签与 Release ===
      - name: Remove Old Release and Tag
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 删除远端 Release 和 Tag (|| true 确保首次运行或找不到标签时不会导致报错中断)
          gh release delete latest --cleanup-tag -y || true
          git push origin :refs/tags/latest || true

      - name: Get Current Time
        run: echo "UPDATE_TIME=$(date +'%Y-%m-%d %H:%M:%S')" >> $GITHUB_ENV

      # === 修改：固定发版到 latest ===
      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: "Latest Rules"
          body: "自动更新时间: ${{ env.UPDATE_TIME }}"
          files: |
            srs_output/*.srs
            srs_output/*.json
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
